import React from 'react';
import Helmet from "react-helmet";

const Account = () => {
    return (
        <div className="ui container">
            <Helmet title="Account" />
            <p>This is account page, coming soon!</p>
        </div>
    )
};

export default Account;
